package com.scientiavitae.CrapsPropsCalculator;

/**
 * Launcher Class is the entry point for this program. It simply calls the Main class. I found this necessary as a
 * workaround for JavaFX not working with Main being the program's starting point.
 *
 * @version 0.1
 * @since 0.1
 */
public class Launcher {
    public static void main(String[] args) {
        Main.main(args);
    }
}
